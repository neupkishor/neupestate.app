{
  "name": "com.neup.estate",
  "version": "1.0.0",
  "main": "expo-router/entry",
  "dependencies": {
    "expo": "~57.0.18",
    "expo-blur": "~57.0.2",
    "expo-constants": "~57.0.16",
    "expo-contacts": "~57.0.4",
    "expo-file-system": "~57.0.6",
    "expo-image-manipulator": "~57.0.16",
    "expo-linking": "~57.0.9",
    "expo-router": "~57.0.19",
    "expo-secure-store": "~57.0.3",
    "expo-splash-screen": "~57.0.8",
    "expo-sqlite": "~57.0.2",
    "expo-status-bar": "~57.0.1",
    "expo-system-ui": "~57.0.3",
    "react": "19.2.3",
    "react-native": "0.86.3",
    "react-native-gesture-handler": "~2.32.0",
    "react-native-reanimated": "4.5.1",
    "react-native-safe-area-context": "~5.7.0",
    "react-native-worklets": "0.10.1"
  },
  "devDependencies": {
    "@types/react": "~19.2.2",
    "babel-preset-expo": "~57.0.0",
    "typescript": "~6.0.3"
  },
  "scripts": {
    "setup": "chmod +x ./setup.sh && ./setup.sh",
    "start": "npm run setup && expo start",
    "android": "expo run:android",
    "ios": "expo run:ios",
    "web": "expo start --web",
    "build:android": "npx expo prebuild --clean && ANDROID_HOME=\"${ANDROID_HOME:-$HOME/Library/Android/sdk}\" ANDROID_SDK_ROOT=\"${ANDROID_SDK_ROOT:-$HOME/Library/Android/sdk}\" sh -c 'mkdir -p generated/and.version && npx eas-cli build --platform android --profile production --local --output generated/and.version/app-release.aab'",
    "build:android.apk": "ANDROID_HOME=\"${ANDROID_HOME:-$HOME/Library/Android/sdk}\" ANDROID_SDK_ROOT=\"${ANDROID_SDK_ROOT:-$HOME/Library/Android/sdk}\" sh -c 'cd android && ./gradlew :app:assembleRelease && mkdir -p ../generated/and.version && mv -f app/build/outputs/apk/release/app-release.apk ../generated/and.version/app-release.apk'",
    "install:android.apk": "ANDROID_HOME=\"${ANDROID_HOME:-$HOME/Library/Android/sdk}\" ANDROID_SDK_ROOT=\"${ANDROID_SDK_ROOT:-$HOME/Library/Android/sdk}\" adb install -r generated/and.version/app-release.apk",
    "build:ios": "mkdir -p generated/ios.version && npx eas build --platform ios --profile production --local --output generated/ios.version/app-release.ipa"
  },
  "private": true
}
